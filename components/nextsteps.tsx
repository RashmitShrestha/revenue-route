"use strict";

// maybe add ideas to what do depending on if the total sum is negative or positive
// for example, if its negative it can offer some loan options
// if its positive it can offer some investment options, using a API to get the latest stock prices
//

// import router from 'next/router';
